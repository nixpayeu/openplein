# OpenPlein Fase 0 (MVP) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Werkende OpenPlein-MVP: PWA-shell die mini-apps in gesandboxte iframes draait met een permissie-gescopede `window.plein`-bridge (identity/storage/payments via Nixpay-provider in Mollie-testmodus), plus SDK-scaffolder en twee demo-mini-apps.

**Architecture:** pnpm-monorepo met drie packages (`runtime` = React-PWA-shell, `bridge` = postMessage-RPC + permissiemodel, `sdk` = manifest-schema + types + `create-plein-app`) en één demo-app (`apps/demo` = backend Hono + twee statische mini-apps). De bridge is het veiligheidskritieke hart en wordt volledig TDD gebouwd; de shell consumeert de bridge als library.

**Tech Stack:** Node ≥20, pnpm ≥9, TypeScript (strict), Vite + React 18, Vitest + jsdom, Hono + @mollie/api-client, Ajv, Playwright.

## Global Constraints

- Node ≥ 20, pnpm ≥ 9; alle packages `"type": "module"`.
- TypeScript strict mode overal (`tsconfig.base.json`).
- Licenties exact per spec: `packages/runtime` en `packages/bridge` AGPL-3.0, `packages/sdk` MIT (LICENSE-bestand per package + `"license"`-veld in package.json).
- UI-copy tweetalig NL/EN via `t()`-dictionaries; geen hardcoded strings in componenten.
- Geen externe CDN's; alles self-hosted (soevereiniteitsclaim).
- Mini-app-iframes altijd `sandbox="allow-scripts allow-forms"` — nooit `allow-same-origin`.
- Betalingen uitsluitend Mollie-**testmodus**; API-key alleen via env (`MOLLIE_API_KEY`), nooit in git.
- Permissienamen exact: `payments`, `identity`, `storage`, `notifications` (laatste is fase 1, wel al in het type).
- Commits door Nick als enige auteur, zonder co-author-trailers.

## Bestandsstructuur (eindbeeld fase 0)

```
openplein/
├── pnpm-workspace.yaml
├── package.json / tsconfig.base.json / vitest.workspace.ts
├── packages/
│   ├── sdk/          # MIT — manifest-schema, types, validateManifest, create-plein-app
│   │   └── src/{manifest.ts,schema.json,cli.ts}
│   ├── bridge/       # AGPL — protocol.ts (envelope+errors), client.ts (window.plein), host.ts (PleinHost)
│   └── runtime/      # AGPL — React-shell: App, HomeScreen, MiniAppView, PermissionDialog, LoginView,
│       └── src/{permissions.ts,providers/{storage.ts,identity.ts,payments.ts},i18n/{nl.json,en.json}}
├── apps/demo/
│   ├── server/       # Hono: /api/auth/*, /api/payments/*, statische hosting
│   └── miniapps/{lijstje,betalen}/   # elk: index.html, app.js, plein.manifest.json
├── e2e/              # Playwright
└── docs/{miniapp-spec.md,funding/}, GOVERNANCE.md, README.md
```

---

### Task 1: Monorepo-scaffold en tooling

**Files:**
- Create: `pnpm-workspace.yaml`, `package.json`, `tsconfig.base.json`, `vitest.workspace.ts`, `.gitignore`, `.nvmrc`

**Interfaces:**
- Produces: workspaces `packages/*` en `apps/*`; root-scripts `pnpm test` (vitest) en `pnpm typecheck`.

- [ ] **Step 1: Schrijf de workspace-bestanden**

`pnpm-workspace.yaml`:
```yaml
packages:
  - "packages/*"
  - "apps/*"
```

`package.json`:
```json
{
  "name": "openplein",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "jsdom": "^24.0.0",
    "@types/node": "^20.14.0"
  }
}
```

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": true,
    "declaration": true,
    "jsx": "react-jsx"
  }
}
```

`vitest.workspace.ts`:
```ts
export default ["packages/*", "apps/demo/server"];
```

`.gitignore`:
```
node_modules/
dist/
.env
*.local
test-results/
```

`.nvmrc`: `20`

- [ ] **Step 2: Installeer en verifieer**

Run: `pnpm install && pnpm vitest run --passWithNoTests`
Expected: "No test files found" + exit 0.

- [ ] **Step 3: Commit**

```bash
git add -A && git commit -m "chore: monorepo-scaffold (pnpm workspaces, vitest, strict TS)"
```

---

### Task 2: SDK — manifest-types, JSON Schema en validateManifest

**Files:**
- Create: `packages/sdk/package.json`, `packages/sdk/tsconfig.json`, `packages/sdk/LICENSE` (MIT), `packages/sdk/src/manifest.ts`, `packages/sdk/src/schema.json`, `packages/sdk/src/index.ts`
- Test: `packages/sdk/src/manifest.test.ts`

**Interfaces:**
- Produces: `type Permission = 'payments'|'identity'|'storage'|'notifications'`; `interface PleinManifest { id; name; version; icon; entry; provider: {name; url}; permissions: Permission[] }`; `validateManifest(data: unknown): { valid: true; manifest: PleinManifest } | { valid: false; errors: string[] }`. Alle latere tasks importeren dit uit `@openplein/sdk`.

- [ ] **Step 1: Package-bestanden**

`packages/sdk/package.json`:
```json
{
  "name": "@openplein/sdk",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "main": "src/index.ts",
  "dependencies": { "ajv": "^8.16.0" }
}
```

`packages/sdk/tsconfig.json`:
```json
{ "extends": "../../tsconfig.base.json", "include": ["src"] }
```

`LICENSE`: standaard MIT-tekst, copyright "2026 Nixpay B.V.".

- [ ] **Step 2: Schrijf de falende test**

`packages/sdk/src/manifest.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { validateManifest } from "./manifest";

const valid = {
  id: "nl.easeo.lijstje", name: "Lijstje", version: "0.1.0",
  icon: "icon.svg", entry: "http://localhost:5180/",
  provider: { name: "EASEO", url: "https://easeo.nl" },
  permissions: ["identity", "storage"],
};

describe("validateManifest", () => {
  it("accepteert een geldig manifest", () => {
    const r = validateManifest(valid);
    expect(r.valid).toBe(true);
    if (r.valid) expect(r.manifest.id).toBe("nl.easeo.lijstje");
  });
  it("weigert ontbrekend id", () => {
    const { id, ...rest } = valid;
    const r = validateManifest(rest);
    expect(r.valid).toBe(false);
    if (!r.valid) expect(r.errors.join()).toContain("id");
  });
  it("weigert onbekende permissie", () => {
    const r = validateManifest({ ...valid, permissions: ["camera"] });
    expect(r.valid).toBe(false);
  });
});
```

- [ ] **Step 3: Run test — moet falen**

Run: `pnpm --filter @openplein/sdk exec vitest run`
Expected: FAIL — "Cannot find module './manifest'".

- [ ] **Step 4: Implementeer schema + validator**

`packages/sdk/src/schema.json`:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["id", "name", "version", "icon", "entry", "provider", "permissions"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9]+(\\.[a-z0-9-]+)+$" },
    "name": { "type": "string", "minLength": 1 },
    "version": { "type": "string" },
    "icon": { "type": "string" },
    "entry": { "type": "string", "format": "uri" },
    "provider": {
      "type": "object", "required": ["name", "url"],
      "properties": { "name": { "type": "string" }, "url": { "type": "string" } }
    },
    "permissions": {
      "type": "array",
      "items": { "enum": ["payments", "identity", "storage", "notifications"] }
    }
  }
}
```

`packages/sdk/src/manifest.ts`:
```ts
import Ajv from "ajv";
import schema from "./schema.json" with { type: "json" };

export const PERMISSIONS = ["payments", "identity", "storage", "notifications"] as const;
export type Permission = (typeof PERMISSIONS)[number];

export interface PleinManifest {
  id: string; name: string; version: string; icon: string; entry: string;
  provider: { name: string; url: string };
  permissions: Permission[];
}

const ajv = new Ajv({ allErrors: true });
const check = ajv.compile(schema);

export function validateManifest(
  data: unknown
): { valid: true; manifest: PleinManifest } | { valid: false; errors: string[] } {
  if (check(data)) return { valid: true, manifest: data as unknown as PleinManifest };
  return {
    valid: false,
    errors: (check.errors ?? []).map((e) => `${e.instancePath || "/"} ${e.message}`),
  };
}
```

`packages/sdk/src/index.ts`:
```ts
export * from "./manifest";
```

- [ ] **Step 5: Run test — moet slagen**

Run: `pnpm install && pnpm --filter @openplein/sdk exec vitest run`
Expected: 3 passed.

- [ ] **Step 6: Commit**

```bash
git add packages/sdk && git commit -m "feat(sdk): manifest-schema, types en validateManifest"
```

---

### Task 3: Bridge — protocol-envelope en errorcodes

**Files:**
- Create: `packages/bridge/package.json`, `packages/bridge/tsconfig.json`, `packages/bridge/LICENSE` (AGPL-3.0), `packages/bridge/src/protocol.ts`, `packages/bridge/src/index.ts`
- Test: `packages/bridge/src/protocol.test.ts`

**Interfaces:**
- Consumes: `Permission` uit `@openplein/sdk` (Task 2).
- Produces:
  - `type BridgeRequest = { plein: "0.1"; id: string; method: string; params?: unknown }`
  - `type BridgeResponse = { plein: "0.1"; id: string; ok: true; result: unknown } | { plein: "0.1"; id: string; ok: false; error: { code: PleinErrorCode; message: string } }`
  - `type PleinErrorCode = "PERMISSION_DENIED" | "UNKNOWN_METHOD" | "PROVIDER_ERROR" | "NOT_AUTHENTICATED" | "TIMEOUT"`
  - `methodPermission(method: string): Permission | null` — mapt `"pay"→"payments"`, `"identity.request"→"identity"`, `"storage.get"/"storage.set"→"storage"`, anders `null`.
  - `isBridgeRequest(data: unknown): data is BridgeRequest`

- [ ] **Step 1: Package-bestanden**

`packages/bridge/package.json`:
```json
{
  "name": "@openplein/bridge",
  "version": "0.1.0",
  "type": "module",
  "license": "AGPL-3.0-only",
  "main": "src/index.ts",
  "dependencies": { "@openplein/sdk": "workspace:*" }
}
```

`tsconfig.json` zoals sdk (extends base, include src). `LICENSE`: AGPL-3.0-tekst, copyright "2026 Nixpay B.V.".

- [ ] **Step 2: Schrijf de falende test**

`packages/bridge/src/protocol.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { isBridgeRequest, methodPermission } from "./protocol";

describe("protocol", () => {
  it("herkent een geldige request-envelope", () => {
    expect(isBridgeRequest({ plein: "0.1", id: "1", method: "pay" })).toBe(true);
  });
  it("weigert envelope zonder plein-versie of id", () => {
    expect(isBridgeRequest({ id: "1", method: "pay" })).toBe(false);
    expect(isBridgeRequest({ plein: "0.1", method: "pay" })).toBe(false);
    expect(isBridgeRequest("nope")).toBe(false);
  });
  it("mapt methods naar permissies", () => {
    expect(methodPermission("pay")).toBe("payments");
    expect(methodPermission("identity.request")).toBe("identity");
    expect(methodPermission("storage.get")).toBe("storage");
    expect(methodPermission("storage.set")).toBe("storage");
    expect(methodPermission("bestaatNiet")).toBe(null);
  });
});
```

- [ ] **Step 3: Run test — moet falen**

Run: `pnpm --filter @openplein/bridge exec vitest run`
Expected: FAIL — module niet gevonden.

- [ ] **Step 4: Implementeer protocol.ts**

```ts
import type { Permission } from "@openplein/sdk";

export const PROTOCOL_VERSION = "0.1" as const;

export type PleinErrorCode =
  | "PERMISSION_DENIED" | "UNKNOWN_METHOD" | "PROVIDER_ERROR"
  | "NOT_AUTHENTICATED" | "TIMEOUT";

export interface BridgeRequest {
  plein: typeof PROTOCOL_VERSION; id: string; method: string; params?: unknown;
}

export type BridgeResponse =
  | { plein: typeof PROTOCOL_VERSION; id: string; ok: true; result: unknown }
  | { plein: typeof PROTOCOL_VERSION; id: string; ok: false;
      error: { code: PleinErrorCode; message: string } };

const METHOD_PERMISSIONS: Record<string, Permission> = {
  pay: "payments",
  "identity.request": "identity",
  "storage.get": "storage",
  "storage.set": "storage",
};

export function methodPermission(method: string): Permission | null {
  return METHOD_PERMISSIONS[method] ?? null;
}

export function isBridgeRequest(data: unknown): data is BridgeRequest {
  return (
    typeof data === "object" && data !== null &&
    (data as BridgeRequest).plein === PROTOCOL_VERSION &&
    typeof (data as BridgeRequest).id === "string" &&
    typeof (data as BridgeRequest).method === "string"
  );
}
```

`src/index.ts`: `export * from "./protocol"; ` (client/host komen er in Task 4/5 bij).

- [ ] **Step 5: Run test — moet slagen**

Run: `pnpm install && pnpm --filter @openplein/bridge exec vitest run`
Expected: 3 passed.

- [ ] **Step 6: Commit**

```bash
git add packages/bridge && git commit -m "feat(bridge): protocol-envelope, errorcodes en method→permissie-mapping"
```

---

### Task 4: Bridge — client (`window.plein`)

**Files:**
- Create: `packages/bridge/src/client.ts`
- Modify: `packages/bridge/src/index.ts` (export toevoegen)
- Test: `packages/bridge/src/client.test.ts` (jsdom)

**Interfaces:**
- Consumes: protocol-types uit Task 3.
- Produces: `createPleinClient(opts?: { target?: Window; timeoutMs?: number }): PleinClient` met
  `interface PleinClient { pay(p: { amount: string; currency: "EUR"; description: string }): Promise<{ status: "paid" | "canceled" | "failed" }>; identity: { request(): Promise<{ email: string }> }; storage: { get(key: string): Promise<string | null>; set(key: string, value: string): Promise<void> } }`.
  Elke call stuurt een `BridgeRequest` via `target.postMessage(msg, "*")` en resolvet op de bijpassende `BridgeResponse`; een `ok: false`-response reject met `PleinError` (met `.code`); geen antwoord binnen `timeoutMs` (default 30000) reject met code `TIMEOUT`. Exporteert ook `class PleinError extends Error { code: PleinErrorCode }`.

- [ ] **Step 1: Schrijf de falende test**

`packages/bridge/src/client.test.ts` — testomgeving jsdom (`// @vitest-environment jsdom`):
```ts
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";
import { createPleinClient, PleinError } from "./client";
import { isBridgeRequest, type BridgeRequest, type BridgeResponse } from "./protocol";

function fakeHost(reply: (req: BridgeRequest) => BridgeResponse["error"] | { result: unknown }) {
  // vangt requests op window en antwoordt zoals een echte host
  window.addEventListener("message", (ev) => {
    if (!isBridgeRequest(ev.data)) return;
    const req = ev.data;
    const r = reply(req);
    const resp: BridgeResponse =
      "result" in r
        ? { plein: "0.1", id: req.id, ok: true, result: r.result }
        : { plein: "0.1", id: req.id, ok: false, error: r };
    window.postMessage(resp, "*");
  });
}

describe("createPleinClient", () => {
  it("resolvet storage.get met het result van de host", async () => {
    fakeHost((req) =>
      req.method === "storage.get" ? { result: "melk" } : { code: "UNKNOWN_METHOD", message: "?" }
    );
    const plein = createPleinClient({ target: window });
    await expect(plein.storage.get("item")).resolves.toBe("melk");
  });
  it("reject met PleinError bij ok:false", async () => {
    fakeHost(() => ({ code: "PERMISSION_DENIED", message: "geweigerd" }));
    const plein = createPleinClient({ target: window });
    const err = await plein.pay({ amount: "1.00", currency: "EUR", description: "x" }).catch((e) => e);
    expect(err).toBeInstanceOf(PleinError);
    expect(err.code).toBe("PERMISSION_DENIED");
  });
  it("reject met TIMEOUT als de host nooit antwoordt", async () => {
    const plein = createPleinClient({ target: window, timeoutMs: 50 });
    const err = await plein.identity.request().catch((e) => e);
    expect(err.code).toBe("TIMEOUT");
  });
});
```

- [ ] **Step 2: Run test — moet falen**

Run: `pnpm --filter @openplein/bridge exec vitest run src/client.test.ts`
Expected: FAIL — `./client` bestaat niet.

- [ ] **Step 3: Implementeer client.ts**

```ts
import {
  PROTOCOL_VERSION, type BridgeRequest, type BridgeResponse, type PleinErrorCode,
} from "./protocol";

export class PleinError extends Error {
  constructor(public code: PleinErrorCode, message: string) { super(message); }
}

export interface PleinClient {
  pay(p: { amount: string; currency: "EUR"; description: string }): Promise<{ status: "paid" | "canceled" | "failed" }>;
  identity: { request(): Promise<{ email: string }> };
  storage: { get(key: string): Promise<string | null>; set(key: string, value: string): Promise<void> };
}

export function createPleinClient(opts: { target?: Window; timeoutMs?: number } = {}): PleinClient {
  const target = opts.target ?? window.parent;
  const timeoutMs = opts.timeoutMs ?? 30_000;
  const pending = new Map<string, { resolve: (v: unknown) => void; reject: (e: PleinError) => void }>();
  let seq = 0;

  window.addEventListener("message", (ev: MessageEvent) => {
    const data = ev.data as BridgeResponse;
    if (typeof data !== "object" || data === null || data.plein !== PROTOCOL_VERSION) return;
    if (!("ok" in data)) return; // requests negeren (alleen responses afhandelen)
    const p = pending.get(data.id);
    if (!p) return;
    pending.delete(data.id);
    if (data.ok) p.resolve(data.result);
    else p.reject(new PleinError(data.error.code, data.error.message));
  });

  function call(method: string, params?: unknown): Promise<unknown> {
    const id = `${Date.now()}-${seq++}`;
    const req: BridgeRequest = { plein: PROTOCOL_VERSION, id, method, params };
    return new Promise((resolve, reject) => {
      pending.set(id, { resolve, reject });
      target.postMessage(req, "*");
      setTimeout(() => {
        if (pending.delete(id)) reject(new PleinError("TIMEOUT", `Geen antwoord op ${method}`));
      }, timeoutMs);
    });
  }

  return {
    pay: (p) => call("pay", p) as Promise<{ status: "paid" | "canceled" | "failed" }>,
    identity: { request: () => call("identity.request") as Promise<{ email: string }> },
    storage: {
      get: (key) => call("storage.get", { key }) as Promise<string | null>,
      set: (key, value) => call("storage.set", { key, value }) as Promise<void>,
    },
  };
}
```

Voeg aan `src/index.ts` toe: `export * from "./client";`

- [ ] **Step 4: Run test — moet slagen**

Run: `pnpm --filter @openplein/bridge exec vitest run`
Expected: 6 passed (3 protocol + 3 client).

- [ ] **Step 5: Commit**

```bash
git add packages/bridge && git commit -m "feat(bridge): window.plein-client met RPC, PleinError en timeout"
```

---

### Task 5: Bridge — host (`PleinHost`) met permissie-gate

**Files:**
- Create: `packages/bridge/src/host.ts`
- Modify: `packages/bridge/src/index.ts` (export toevoegen)
- Test: `packages/bridge/src/host.test.ts` (jsdom)

**Interfaces:**
- Consumes: protocol (Task 3), `PleinManifest` (Task 2).
- Produces:
  ```ts
  interface Providers {
    pay(appId: string, params: unknown): Promise<unknown>;
    identityRequest(appId: string): Promise<{ email: string }>;
    storageGet(appId: string, key: string): Promise<string | null>;
    storageSet(appId: string, key: string, value: string): Promise<void>;
  }
  type PermissionGate = (appId: string, permission: Permission) => Promise<boolean>;
  class PleinHost {
    constructor(opts: { manifest: PleinManifest; source: Window; gate: PermissionGate; providers: Providers });
    start(): void;   // registreert message-listener
    stop(): void;    // verwijdert listener (bij sluiten mini-app)
  }
  ```
  Gedrag: alleen berichten van `opts.source` met geldige envelope worden verwerkt; origin van het event moet gelijk zijn aan `new URL(manifest.entry).origin` (behalve `"null"` bij sandboxed iframes zonder allow-same-origin — dat is onze standaard, dus `"null"` is toegestaan mits `ev.source === opts.source`). Method zonder mapping → `UNKNOWN_METHOD`; permissie niet in `manifest.permissions` óf gate geeft `false` → `PERMISSION_DENIED`; provider gooit → `PROVIDER_ERROR` (of `NOT_AUTHENTICATED` als de provider een `PleinError` met die code gooit). Antwoord gaat via `opts.source.postMessage(resp, "*")`.

- [ ] **Step 1: Schrijf de falende test**

`packages/bridge/src/host.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect, vi } from "vitest";
import { PleinHost } from "./host";
import type { BridgeResponse } from "./protocol";
import type { PleinManifest } from "@openplein/sdk";

const manifest: PleinManifest = {
  id: "nl.easeo.test", name: "Test", version: "0.1.0", icon: "i.svg",
  entry: "http://localhost:5180/", provider: { name: "EASEO", url: "https://easeo.nl" },
  permissions: ["storage"],
};

function makeHost(gateAnswer: boolean, providers: Partial<Record<string, unknown>> = {}) {
  const sent: BridgeResponse[] = [];
  const source = { postMessage: (m: BridgeResponse) => sent.push(m) } as unknown as Window;
  const host = new PleinHost({
    manifest, source, gate: async () => gateAnswer,
    providers: {
      pay: vi.fn(), identityRequest: vi.fn(),
      storageGet: vi.fn(async () => "melk"), storageSet: vi.fn(async () => {}),
      ...providers,
    } as never,
  });
  host.start();
  return { sent, source };
}

function deliver(source: Window, data: unknown) {
  window.dispatchEvent(new MessageEvent("message", { data, origin: "null", source }));
}

async function flush() { await new Promise((r) => setTimeout(r, 0)); }

describe("PleinHost", () => {
  it("beantwoordt storage.get via provider als permissie gegund is", async () => {
    const { sent, source } = makeHost(true);
    deliver(source, { plein: "0.1", id: "a", method: "storage.get", params: { key: "x" } });
    await flush();
    expect(sent[0]).toEqual({ plein: "0.1", id: "a", ok: true, result: "melk" });
  });
  it("geeft PERMISSION_DENIED als de gate weigert", async () => {
    const { sent, source } = makeHost(false);
    deliver(source, { plein: "0.1", id: "b", method: "storage.get", params: { key: "x" } });
    await flush();
    expect(sent[0]).toMatchObject({ ok: false, error: { code: "PERMISSION_DENIED" } });
  });
  it("geeft PERMISSION_DENIED voor permissie buiten het manifest (pay)", async () => {
    const { sent, source } = makeHost(true);
    deliver(source, { plein: "0.1", id: "c", method: "pay", params: {} });
    await flush();
    expect(sent[0]).toMatchObject({ ok: false, error: { code: "PERMISSION_DENIED" } });
  });
  it("geeft UNKNOWN_METHOD voor onbekende methods", async () => {
    const { sent, source } = makeHost(true);
    deliver(source, { plein: "0.1", id: "d", method: "yolo" });
    await flush();
    expect(sent[0]).toMatchObject({ ok: false, error: { code: "UNKNOWN_METHOD" } });
  });
  it("negeert berichten van een andere source", async () => {
    const { sent } = makeHost(true);
    deliver({} as Window, { plein: "0.1", id: "e", method: "storage.get", params: { key: "x" } });
    await flush();
    expect(sent).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Run test — moet falen**

Run: `pnpm --filter @openplein/bridge exec vitest run src/host.test.ts`
Expected: FAIL — `./host` bestaat niet.

- [ ] **Step 3: Implementeer host.ts**

```ts
import type { PleinManifest, Permission } from "@openplein/sdk";
import {
  PROTOCOL_VERSION, isBridgeRequest, methodPermission,
  type BridgeRequest, type BridgeResponse,
} from "./protocol";
import { PleinError } from "./client";

export interface Providers {
  pay(appId: string, params: unknown): Promise<unknown>;
  identityRequest(appId: string): Promise<{ email: string }>;
  storageGet(appId: string, key: string): Promise<string | null>;
  storageSet(appId: string, key: string, value: string): Promise<void>;
}

export type PermissionGate = (appId: string, permission: Permission) => Promise<boolean>;

export class PleinHost {
  private listener?: (ev: MessageEvent) => void;

  constructor(private opts: {
    manifest: PleinManifest; source: Window; gate: PermissionGate; providers: Providers;
  }) {}

  start(): void {
    const allowedOrigin = new URL(this.opts.manifest.entry).origin;
    this.listener = (ev: MessageEvent) => {
      if (ev.source !== this.opts.source) return;
      // sandboxed iframes zonder allow-same-origin hebben origin "null"
      if (ev.origin !== "null" && ev.origin !== allowedOrigin) return;
      if (!isBridgeRequest(ev.data)) return;
      void this.handle(ev.data);
    };
    window.addEventListener("message", this.listener);
  }

  stop(): void {
    if (this.listener) window.removeEventListener("message", this.listener);
  }

  private respond(id: string, body: Omit<BridgeResponse, "plein" | "id">): void {
    this.opts.source.postMessage({ plein: PROTOCOL_VERSION, id, ...body } as BridgeResponse, "*");
  }

  private async handle(req: BridgeRequest): Promise<void> {
    const { manifest, gate, providers } = this.opts;
    const permission = methodPermission(req.method);
    if (!permission) {
      return this.respond(req.id, { ok: false, error: { code: "UNKNOWN_METHOD", message: req.method } });
    }
    if (!manifest.permissions.includes(permission) || !(await gate(manifest.id, permission))) {
      return this.respond(req.id, {
        ok: false, error: { code: "PERMISSION_DENIED", message: `${permission} geweigerd` },
      });
    }
    try {
      const p = (req.params ?? {}) as { key?: string; value?: string };
      let result: unknown;
      switch (req.method) {
        case "pay": result = await providers.pay(manifest.id, req.params); break;
        case "identity.request": result = await providers.identityRequest(manifest.id); break;
        case "storage.get": result = await providers.storageGet(manifest.id, String(p.key)); break;
        case "storage.set": result = await providers.storageSet(manifest.id, String(p.key), String(p.value)); break;
      }
      this.respond(req.id, { ok: true, result });
    } catch (e) {
      const code = e instanceof PleinError ? e.code : "PROVIDER_ERROR";
      this.respond(req.id, {
        ok: false, error: { code, message: e instanceof Error ? e.message : "provider-fout" },
      });
    }
  }
}
```

Voeg aan `src/index.ts` toe: `export * from "./host";`

- [ ] **Step 4: Run test — moet slagen**

Run: `pnpm --filter @openplein/bridge exec vitest run`
Expected: 11 passed.

- [ ] **Step 5: Commit**

```bash
git add packages/bridge && git commit -m "feat(bridge): PleinHost met origin-check, permissie-gate en provider-dispatch"
```

---

### Task 6: Runtime — PermissionStore en lokale providers (storage, identity)

**Files:**
- Create: `packages/runtime/package.json`, `packages/runtime/tsconfig.json`, `packages/runtime/LICENSE` (AGPL-3.0), `packages/runtime/src/permissions.ts`, `packages/runtime/src/providers/storage.ts`, `packages/runtime/src/providers/identity.ts`
- Test: `packages/runtime/src/permissions.test.ts`, `packages/runtime/src/providers/storage.test.ts` (jsdom)

**Interfaces:**
- Consumes: `Permission` (Task 2), `PleinError` (Task 4).
- Produces:
  - `class PermissionStore { decision(appId, p: Permission): "granted" | "denied" | "unset"; grant(appId, p): void; deny(appId, p): void }` — persistent in `localStorage` onder key `plein.permissions` (JSON: `{ [appId]: { [permission]: boolean } }`).
  - `storageProvider = { get(appId, key), set(appId, key, value) }` — localStorage-keys `plein.store.<appId>.<key>`.
  - `identityProvider(getSession: () => { email: string } | null)` → object met `request(appId)`: geeft `{ email }` terug of gooit `PleinError("NOT_AUTHENTICATED", ...)`.

- [ ] **Step 1: Package-bestanden**

`packages/runtime/package.json`:
```json
{
  "name": "@openplein/runtime",
  "version": "0.1.0",
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": { "dev": "vite", "build": "vite build" },
  "dependencies": {
    "@openplein/bridge": "workspace:*",
    "@openplein/sdk": "workspace:*",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "vite-plugin-pwa": "^0.20.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  }
}
```

`tsconfig.json` als bij sdk. `LICENSE`: AGPL-3.0, copyright "2026 Nixpay B.V.".

- [ ] **Step 2: Schrijf de falende tests**

`packages/runtime/src/permissions.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect, beforeEach } from "vitest";
import { PermissionStore } from "./permissions";

describe("PermissionStore", () => {
  beforeEach(() => localStorage.clear());
  it("is unset voor onbekende app/permissie", () => {
    expect(new PermissionStore().decision("a", "storage")).toBe("unset");
  });
  it("onthoudt grant en deny over instanties heen (persistentie)", () => {
    const s = new PermissionStore();
    s.grant("a", "storage"); s.deny("a", "payments");
    const s2 = new PermissionStore();
    expect(s2.decision("a", "storage")).toBe("granted");
    expect(s2.decision("a", "payments")).toBe("denied");
    expect(s2.decision("b", "storage")).toBe("unset");
  });
});
```

`packages/runtime/src/providers/storage.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect, beforeEach } from "vitest";
import { storageProvider } from "./storage";

describe("storageProvider", () => {
  beforeEach(() => localStorage.clear());
  it("scheidt data per appId", async () => {
    await storageProvider.set("app.a", "k", "1");
    await storageProvider.set("app.b", "k", "2");
    expect(await storageProvider.get("app.a", "k")).toBe("1");
    expect(await storageProvider.get("app.b", "k")).toBe("2");
    expect(await storageProvider.get("app.c", "k")).toBe(null);
  });
});
```

- [ ] **Step 3: Run tests — moeten falen**

Run: `pnpm --filter @openplein/runtime exec vitest run`
Expected: FAIL — modules bestaan niet.

- [ ] **Step 4: Implementeer**

`src/permissions.ts`:
```ts
import type { Permission } from "@openplein/sdk";

const KEY = "plein.permissions";
type Stored = Record<string, Partial<Record<Permission, boolean>>>;

export class PermissionStore {
  private read(): Stored {
    try { return JSON.parse(localStorage.getItem(KEY) ?? "{}") as Stored; }
    catch { return {}; }
  }
  private write(s: Stored): void { localStorage.setItem(KEY, JSON.stringify(s)); }

  decision(appId: string, p: Permission): "granted" | "denied" | "unset" {
    const v = this.read()[appId]?.[p];
    return v === undefined ? "unset" : v ? "granted" : "denied";
  }
  grant(appId: string, p: Permission): void {
    const s = this.read(); (s[appId] ??= {})[p] = true; this.write(s);
  }
  deny(appId: string, p: Permission): void {
    const s = this.read(); (s[appId] ??= {})[p] = false; this.write(s);
  }
}
```

`src/providers/storage.ts`:
```ts
export const storageProvider = {
  async get(appId: string, key: string): Promise<string | null> {
    return localStorage.getItem(`plein.store.${appId}.${key}`);
  },
  async set(appId: string, key: string, value: string): Promise<void> {
    localStorage.setItem(`plein.store.${appId}.${key}`, value);
  },
};
```

`src/providers/identity.ts`:
```ts
import { PleinError } from "@openplein/bridge";

export function identityProvider(getSession: () => { email: string } | null) {
  return {
    async request(_appId: string): Promise<{ email: string }> {
      const session = getSession();
      if (!session) throw new PleinError("NOT_AUTHENTICATED", "Niet ingelogd in Plein");
      return { email: session.email };
    },
  };
}
```

- [ ] **Step 5: Run tests — moeten slagen**

Run: `pnpm install && pnpm --filter @openplein/runtime exec vitest run`
Expected: 3 passed.

- [ ] **Step 6: Commit**

```bash
git add packages/runtime && git commit -m "feat(runtime): PermissionStore + storage/identity-providers"
```

---

### Task 7: Runtime — shell-UI (home, catalogus, mini-app-view, permissiedialoog, login, i18n)

**Files:**
- Create: `packages/runtime/index.html`, `packages/runtime/vite.config.ts`, `packages/runtime/src/main.tsx`, `packages/runtime/src/App.tsx`, `packages/runtime/src/i18n/nl.json`, `packages/runtime/src/i18n/en.json`, `packages/runtime/src/i18n/index.ts`, `packages/runtime/src/components/HomeScreen.tsx`, `packages/runtime/src/components/MiniAppView.tsx`, `packages/runtime/src/components/PermissionDialog.tsx`, `packages/runtime/src/components/LoginView.tsx`, `packages/runtime/src/catalog.ts`, `packages/runtime/public/catalog.json`, `packages/runtime/src/styles.css`
- Test: `packages/runtime/src/i18n/i18n.test.ts`

**Interfaces:**
- Consumes: `PleinHost`, `Providers`, `PermissionGate` (Task 5); `PermissionStore`, providers (Task 6); `validateManifest` (Task 2); `paymentsProvider` (Task 8 — in deze task nog een stub die `PROVIDER_ERROR` gooit, Task 8 vervangt hem).
- Produces: draaiende shell op `http://localhost:5173` (`pnpm --filter @openplein/runtime dev`); `loadCatalog(): Promise<PleinManifest[]>` (fetcht `/catalog.json`, valideert elk item met `validateManifest`, slaat ongeldige over met `console.warn`); `t(key: string): string` + `setLocale("nl" | "en")` met browservoorkeur als default.

Kernpunten (volledige code in de task hieronder):

- [ ] **Step 1: Falende i18n-test**

`src/i18n/i18n.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";
import { t, setLocale } from "./index";

describe("i18n", () => {
  it("vertaalt in beide talen en valt terug op de key", () => {
    setLocale("nl"); expect(t("home.title")).toBe("Plein");
    setLocale("en"); expect(t("home.discover")).toBe("Discover");
    expect(t("bestaat.niet")).toBe("bestaat.niet");
  });
});
```

- [ ] **Step 2: Run — FAIL** (`./index` bestaat niet), daarna implementeer i18n:

`src/i18n/nl.json`:
```json
{
  "home.title": "Plein", "home.discover": "Ontdekken", "home.installed": "Jouw mini-apps",
  "login.title": "Inloggen bij Plein", "login.email": "E-mailadres", "login.code": "Code uit je mail",
  "login.sendCode": "Stuur code", "login.verify": "Inloggen", "login.error": "Inloggen mislukt",
  "perm.title": "Toestemming gevraagd", "perm.body": "{app} vraagt toegang tot: {permission}",
  "perm.allow": "Toestaan", "perm.deny": "Weigeren",
  "perm.payments": "betalingen", "perm.identity": "je identiteit (e-mailadres)",
  "perm.storage": "opslag in deze app", "perm.notifications": "meldingen",
  "miniapp.close": "Sluiten", "miniapp.by": "door {provider}"
}
```

`src/i18n/en.json`:
```json
{
  "home.title": "Plein", "home.discover": "Discover", "home.installed": "Your mini-apps",
  "login.title": "Sign in to Plein", "login.email": "Email address", "login.code": "Code from your email",
  "login.sendCode": "Send code", "login.verify": "Sign in", "login.error": "Sign-in failed",
  "perm.title": "Permission requested", "perm.body": "{app} requests access to: {permission}",
  "perm.allow": "Allow", "perm.deny": "Deny",
  "perm.payments": "payments", "perm.identity": "your identity (email address)",
  "perm.storage": "storage in this app", "perm.notifications": "notifications",
  "miniapp.close": "Close", "miniapp.by": "by {provider}"
}
```

`src/i18n/index.ts`:
```ts
import nl from "./nl.json" with { type: "json" };
import en from "./en.json" with { type: "json" };

const dicts: Record<string, Record<string, string>> = { nl, en };
let locale: "nl" | "en" =
  typeof navigator !== "undefined" && navigator.language.startsWith("nl") ? "nl" : "en";

export function setLocale(l: "nl" | "en"): void { locale = l; }
export function getLocale(): "nl" | "en" { return locale; }
export function t(key: string, vars: Record<string, string> = {}): string {
  let s = dicts[locale][key] ?? key;
  for (const [k, v] of Object.entries(vars)) s = s.replace(`{${k}}`, v);
  return s;
}
```

Run: `pnpm --filter @openplein/runtime exec vitest run` → 4 passed.

- [ ] **Step 3: Vite-config + PWA + entry**

`vite.config.ts`:
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import { VitePWA } from "vite-plugin-pwa";

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: "autoUpdate",
      manifest: {
        name: "Plein", short_name: "Plein",
        description: "Jouw diensten, één Plein",
        theme_color: "#1a3c6e", background_color: "#ffffff", display: "standalone",
        icons: [{ src: "icon-512.png", sizes: "512x512", type: "image/png" }],
      },
    }),
  ],
  server: { proxy: { "/api": "http://localhost:5175" } },
});
```

`index.html`: standaard Vite-html met `<div id="root">` en `<script type="module" src="/src/main.tsx">`; `<title>Plein</title>`, `<meta name="viewport" content="width=device-width, initial-scale=1">`. Plaats een placeholder `public/icon-512.png` (mag een simpel gegenereerd PNG zijn: `convert -size 512x512 xc:"#1a3c6e" public/icon-512.png` of een leeg blauw vierkant).

`src/main.tsx`:
```tsx
import { createRoot } from "react-dom/client";
import { App } from "./App";
import "./styles.css";

createRoot(document.getElementById("root")!).render(<App />);
```

- [ ] **Step 4: Catalogus**

`public/catalog.json`:
```json
[
  {
    "id": "nl.easeo.lijstje", "name": "Lijstje", "version": "0.1.0",
    "icon": "http://localhost:5180/icon.svg", "entry": "http://localhost:5180/",
    "provider": { "name": "EASEO", "url": "https://easeo.nl" },
    "permissions": ["identity", "storage"]
  },
  {
    "id": "nl.nixpay.betalen", "name": "Betaal-demo", "version": "0.1.0",
    "icon": "http://localhost:5181/icon.svg", "entry": "http://localhost:5181/",
    "provider": { "name": "Nixpay", "url": "https://nixpay.eu" },
    "permissions": ["payments", "identity"]
  }
]
```

`src/catalog.ts`:
```ts
import { validateManifest, type PleinManifest } from "@openplein/sdk";

export async function loadCatalog(): Promise<PleinManifest[]> {
  const res = await fetch("/catalog.json");
  const items = (await res.json()) as unknown[];
  const out: PleinManifest[] = [];
  for (const item of items) {
    const r = validateManifest(item);
    if (r.valid) out.push(r.manifest);
    else console.warn("Ongeldig manifest overgeslagen:", r.errors);
  }
  return out;
}
```

- [ ] **Step 5: App-shell met login, permissiedialoog en mini-app-view**

`src/App.tsx`:
```tsx
import { useEffect, useState, useCallback } from "react";
import type { PleinManifest, Permission } from "@openplein/sdk";
import { loadCatalog } from "./catalog";
import { PermissionStore } from "./permissions";
import { HomeScreen } from "./components/HomeScreen";
import { MiniAppView } from "./components/MiniAppView";
import { PermissionDialog } from "./components/PermissionDialog";
import { LoginView } from "./components/LoginView";

export interface Session { email: string; token: string }
const permissionStore = new PermissionStore();

interface PermissionRequest {
  app: PleinManifest; permission: Permission; resolve: (ok: boolean) => void;
}

export function App() {
  const [catalog, setCatalog] = useState<PleinManifest[]>([]);
  const [session, setSession] = useState<Session | null>(() => {
    const raw = localStorage.getItem("plein.session");
    return raw ? (JSON.parse(raw) as Session) : null;
  });
  const [active, setActive] = useState<PleinManifest | null>(null);
  const [permReq, setPermReq] = useState<PermissionRequest | null>(null);

  useEffect(() => { void loadCatalog().then(setCatalog); }, []);

  const gate = useCallback(async (app: PleinManifest, permission: Permission) => {
    const d = permissionStore.decision(app.id, permission);
    if (d !== "unset") return d === "granted";
    return new Promise<boolean>((resolve) => setPermReq({ app, permission, resolve }));
  }, []);

  const answerPermission = (ok: boolean) => {
    if (!permReq) return;
    if (ok) permissionStore.grant(permReq.app.id, permReq.permission);
    else permissionStore.deny(permReq.app.id, permReq.permission);
    permReq.resolve(ok);
    setPermReq(null);
  };

  const login = (s: Session) => {
    localStorage.setItem("plein.session", JSON.stringify(s));
    setSession(s);
  };

  if (!session) return <LoginView onLogin={login} />;
  return (
    <>
      {active ? (
        <MiniAppView app={active} session={session} gate={gate} onClose={() => setActive(null)} />
      ) : (
        <HomeScreen catalog={catalog} onOpen={setActive} />
      )}
      {permReq && (
        <PermissionDialog
          appName={permReq.app.name} permission={permReq.permission}
          onAnswer={answerPermission}
        />
      )}
    </>
  );
}
```

`src/components/HomeScreen.tsx`:
```tsx
import type { PleinManifest } from "@openplein/sdk";
import { t } from "../i18n";

export function HomeScreen(props: {
  catalog: PleinManifest[]; onOpen: (app: PleinManifest) => void;
}) {
  return (
    <main className="home">
      <h1>{t("home.title")}</h1>
      <h2>{t("home.discover")}</h2>
      <div className="grid">
        {props.catalog.map((app) => (
          <button key={app.id} className="tile" onClick={() => props.onOpen(app)}>
            <img src={app.icon} alt="" width={48} height={48} />
            <span>{app.name}</span>
            <small>{t("miniapp.by", { provider: app.provider.name })}</small>
          </button>
        ))}
      </div>
    </main>
  );
}
```

`src/components/PermissionDialog.tsx`:
```tsx
import type { Permission } from "@openplein/sdk";
import { t } from "../i18n";

export function PermissionDialog(props: {
  appName: string; permission: Permission; onAnswer: (ok: boolean) => void;
}) {
  return (
    <div className="dialog-backdrop" role="dialog" aria-modal="true">
      <div className="dialog">
        <h3>{t("perm.title")}</h3>
        <p>{t("perm.body", { app: props.appName, permission: t(`perm.${props.permission}`) })}</p>
        <div className="dialog-actions">
          <button onClick={() => props.onAnswer(false)}>{t("perm.deny")}</button>
          <button className="primary" onClick={() => props.onAnswer(true)}>{t("perm.allow")}</button>
        </div>
      </div>
    </div>
  );
}
```

`src/components/MiniAppView.tsx` — hier komt bridge en sandbox samen:
```tsx
import { useEffect, useRef } from "react";
import { PleinHost } from "@openplein/bridge";
import type { PleinManifest, Permission } from "@openplein/sdk";
import type { Session } from "../App";
import { storageProvider } from "../providers/storage";
import { identityProvider } from "../providers/identity";
import { paymentsProvider } from "../providers/payments";
import { t } from "../i18n";

export function MiniAppView(props: {
  app: PleinManifest; session: Session;
  gate: (app: PleinManifest, p: Permission) => Promise<boolean>;
  onClose: () => void;
}) {
  const iframeRef = useRef<HTMLIFrameElement>(null);

  useEffect(() => {
    const frame = iframeRef.current;
    if (!frame?.contentWindow) return;
    const identity = identityProvider(() => props.session);
    const host = new PleinHost({
      manifest: props.app,
      source: frame.contentWindow,
      gate: (_appId, p) => props.gate(props.app, p),
      providers: {
        pay: (appId, params) => paymentsProvider.pay(appId, params, props.session.token),
        identityRequest: (appId) => identity.request(appId),
        storageGet: (appId, key) => storageProvider.get(appId, key),
        storageSet: (appId, key, value) => storageProvider.set(appId, key, value),
      },
    });
    host.start();
    return () => host.stop();
  }, [props.app, props.session, props.gate]);

  return (
    <div className="miniapp">
      <header>
        <button onClick={props.onClose}>← {t("miniapp.close")}</button>
        <span>{props.app.name}</span>
      </header>
      <iframe
        ref={iframeRef} src={props.app.entry} title={props.app.name}
        sandbox="allow-scripts allow-forms"
      />
    </div>
  );
}
```

`src/components/LoginView.tsx`:
```tsx
import { useState } from "react";
import type { Session } from "../App";
import { t } from "../i18n";

export function LoginView(props: { onLogin: (s: Session) => void }) {
  const [email, setEmail] = useState("");
  const [code, setCode] = useState("");
  const [stage, setStage] = useState<"email" | "code">("email");
  const [error, setError] = useState(false);

  async function sendCode() {
    const res = await fetch("/api/auth/request-code", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email }),
    });
    if (res.ok) { setError(false); setStage("code"); } else setError(true);
  }
  async function verify() {
    const res = await fetch("/api/auth/verify", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email, code }),
    });
    if (!res.ok) return setError(true);
    const { token } = (await res.json()) as { token: string };
    props.onLogin({ email, token });
  }

  return (
    <main className="login">
      <h1>{t("login.title")}</h1>
      {stage === "email" ? (
        <>
          <label>{t("login.email")}
            <input type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          </label>
          <button className="primary" onClick={() => void sendCode()}>{t("login.sendCode")}</button>
        </>
      ) : (
        <>
          <label>{t("login.code")}
            <input value={code} onChange={(e) => setCode(e.target.value)} />
          </label>
          <button className="primary" onClick={() => void verify()}>{t("login.verify")}</button>
        </>
      )}
      {error && <p className="error">{t("login.error")}</p>}
    </main>
  );
}
```

`src/providers/payments.ts` — stub in deze task (Task 8 vervangt de body):
```ts
export const paymentsProvider = {
  async pay(_appId: string, _params: unknown, _token: string): Promise<never> {
    throw new Error("payments-provider nog niet geconfigureerd");
  },
};
```

`src/styles.css` (geen frameworks, geen CDN):
```css
* { box-sizing: border-box; }
body { margin: 0; font-family: system-ui, -apple-system, sans-serif; color: #1c1c1e; }
.home, .login { padding: 1rem; max-width: 480px; margin: 0 auto; }
.grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: .75rem; }
.tile { display: flex; flex-direction: column; align-items: center; gap: .25rem;
  padding: .75rem; border: 1px solid #e0e0e5; border-radius: 12px; background: #fff; cursor: pointer; }
.tile small { color: #6e6e73; }
button.primary { background: #1a3c6e; color: #fff; border: 0; border-radius: 8px;
  padding: .6rem 1.2rem; font-size: 1rem; cursor: pointer; }
.login label { display: block; margin: 1rem 0 .5rem; }
.login input { width: 100%; padding: .6rem; font-size: 1rem; border: 1px solid #c7c7cc; border-radius: 8px; }
.error { color: #b00020; }
.miniapp header { display: flex; align-items: center; gap: 1rem; height: 48px; padding: 0 .5rem;
  border-bottom: 1px solid #e0e0e5; }
.miniapp iframe { width: 100%; height: calc(100vh - 48px); border: 0; }
.dialog-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,.4);
  display: flex; align-items: center; justify-content: center; }
.dialog { background: #fff; border-radius: 12px; padding: 1.5rem; max-width: 320px; }
.dialog-actions { display: flex; gap: .75rem; justify-content: flex-end; margin-top: 1rem; }
```

- [ ] **Step 6: Handmatige verificatie**

Run: `pnpm --filter @openplein/runtime dev` en open `http://localhost:5173`.
Expected: loginscherm verschijnt (inloggen kan pas na Task 8); typecheck groen via `pnpm typecheck`.

- [ ] **Step 7: Commit**

```bash
git add packages/runtime && git commit -m "feat(runtime): shell-UI met catalogus, sandbox-iframe, permissiedialoog en NL/EN"
```

---

### Task 8: Demo-backend — auth (magic-code) en betalingen (Mollie-test, met mock-modus)

**Files:**
- Create: `apps/demo/server/package.json`, `apps/demo/server/tsconfig.json`, `apps/demo/server/src/app.ts`, `apps/demo/server/src/index.ts`
- Modify: `packages/runtime/src/providers/payments.ts` (stub vervangen)
- Test: `apps/demo/server/src/app.test.ts`

**Interfaces:**
- Consumes: niets uit andere packages (standalone Hono-app); runtime praat ertegen via `/api`-proxy (Task 7).
- Produces (HTTP, poort 5175):
  - `POST /api/auth/request-code` `{ email }` → 204; genereert 6-cijferige code (10 min geldig, in-memory Map), logt hem naar stdout (`[plein-auth] code voor <email>: <code>`); als `SMTP_URL` gezet is óók mailen (nodemailer) — mail is optioneel, stdout is de MVP-flow.
  - `POST /api/auth/verify` `{ email, code }` → `{ token }` (HMAC-SHA256 over email+timestamp met `AUTH_SECRET`, formaat `base64url(email|ts).base64url(hmac)`) of 401.
  - `POST /api/payments` (Bearer token) `{ amount, currency: "EUR", description, appId }` → `{ id, checkoutUrl }`. Met `PAYMENTS_MOCK=1`: fake `id` en `checkoutUrl: "/mock-checkout?id=..."` en de betaling staat na 1 poll op `paid`. Zonder mock: `@mollie/api-client` `payments.create` met `MOLLIE_API_KEY` (test-key), `redirectUrl` naar `PUBLIC_URL`.
  - `GET /api/payments/:id` (Bearer) → `{ status: "open" | "paid" | "canceled" | "failed" | "expired" }` — mock: uit in-memory Map; echt: live opvragen bij Mollie (geen eigen opslag, YAGNI).
  - Alle `/api/payments*`-routes eisen een geldig token (zelfde HMAC-check), anders 401.
- Produces (runtime): `paymentsProvider.pay(appId, params, token)` → POST `/api/payments`, opent `checkoutUrl` via `window.open`, pollt `GET /api/payments/:id` elke 2 s (max 5 min) tot status ≠ `open`, resolvet `{ status }` (waarbij `expired` → `failed` gemapt wordt naar het bridge-contract uit Task 4).

- [ ] **Step 1: Package-bestanden**

`apps/demo/server/package.json`:
```json
{
  "name": "@openplein/demo-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": { "dev": "tsx watch src/index.ts", "start": "tsx src/index.ts" },
  "dependencies": {
    "hono": "^4.4.0",
    "@hono/node-server": "^1.11.0",
    "@mollie/api-client": "^3.7.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": { "tsx": "^4.15.0", "@types/nodemailer": "^6.4.0", "vitest": "^2.0.0" }
}
```

- [ ] **Step 2: Schrijf de falende test** (mock-modus, geen Mollie nodig)

`apps/demo/server/src/app.test.ts`:
```ts
import { describe, it, expect, beforeAll } from "vitest";
import { createApp } from "./app";

const app = createApp({ authSecret: "test-secret", paymentsMock: true });
let token = "";

beforeAll(async () => {
  await app.request("/api/auth/request-code", {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email: "nick@example.nl" }),
  });
  const code = app.debugLastCode!; // test-only accessor, zie implementatie
  const res = await app.request("/api/auth/verify", {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email: "nick@example.nl", code }),
  });
  token = ((await res.json()) as { token: string }).token;
});

describe("auth", () => {
  it("weigert verify met fout code", async () => {
    const res = await app.request("/api/auth/verify", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email: "nick@example.nl", code: "000000" }),
    });
    expect(res.status).toBe(401);
  });
});

describe("payments (mock)", () => {
  it("weigert zonder token", async () => {
    const res = await app.request("/api/payments", { method: "POST" });
    expect(res.status).toBe(401);
  });
  it("maakt een mock-betaling en zet hem op paid na één poll", async () => {
    const create = await app.request("/api/payments", {
      method: "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify({ amount: "2.50", currency: "EUR", description: "test", appId: "nl.nixpay.betalen" }),
    });
    expect(create.status).toBe(200);
    const { id, checkoutUrl } = (await create.json()) as { id: string; checkoutUrl: string };
    expect(checkoutUrl).toContain(id);
    const poll = await app.request(`/api/payments/${id}`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    expect(((await poll.json()) as { status: string }).status).toBe("paid");
  });
});
```

- [ ] **Step 3: Run — FAIL**, dan implementeer `src/app.ts`

```ts
import { Hono } from "hono";
import { createHmac, randomInt, timingSafeEqual } from "node:crypto";

interface Options { authSecret: string; paymentsMock: boolean; mollieApiKey?: string; publicUrl?: string }

type App = Hono & { debugLastCode?: string };

export function createApp(opts: Options): App {
  const app = new Hono() as App;
  const codes = new Map<string, { code: string; expires: number }>();
  const mockPayments = new Map<string, { polls: number }>();

  const sign = (email: string, ts: number) => {
    const payload = Buffer.from(`${email}|${ts}`).toString("base64url");
    const mac = createHmac("sha256", opts.authSecret).update(payload).digest("base64url");
    return `${payload}.${mac}`;
  };
  const verifyToken = (token: string | undefined): boolean => {
    if (!token) return false;
    const [payload, mac] = token.split(".");
    if (!payload || !mac) return false;
    const expected = createHmac("sha256", opts.authSecret).update(payload).digest("base64url");
    try { return timingSafeEqual(Buffer.from(mac), Buffer.from(expected)); }
    catch { return false; }
  };

  app.post("/api/auth/request-code", async (c) => {
    const { email } = await c.req.json<{ email: string }>();
    if (!email?.includes("@")) return c.body(null, 400);
    const code = String(randomInt(100000, 999999));
    codes.set(email, { code, expires: Date.now() + 10 * 60_000 });
    app.debugLastCode = code;
    console.log(`[plein-auth] code voor ${email}: ${code}`);
    // optioneel: SMTP_URL → nodemailer.sendMail; stdout blijft de primaire MVP-flow
    return c.body(null, 204);
  });

  app.post("/api/auth/verify", async (c) => {
    const { email, code } = await c.req.json<{ email: string; code: string }>();
    const entry = codes.get(email);
    if (!entry || entry.expires < Date.now() || entry.code !== code) return c.body(null, 401);
    codes.delete(email);
    return c.json({ token: sign(email, Date.now()) });
  });

  app.use("/api/payments/*", async (c, next) => {
    const auth = c.req.header("Authorization");
    if (!verifyToken(auth?.replace(/^Bearer /, ""))) return c.body(null, 401);
    await next();
  });
  app.use("/api/payments", async (c, next) => {
    const auth = c.req.header("Authorization");
    if (!verifyToken(auth?.replace(/^Bearer /, ""))) return c.body(null, 401);
    await next();
  });

  app.post("/api/payments", async (c) => {
    const body = await c.req.json<{ amount: string; description: string; appId: string }>();
    if (opts.paymentsMock) {
      const id = `mock_${Date.now()}`;
      mockPayments.set(id, { polls: 0 });
      return c.json({ id, checkoutUrl: `/mock-checkout?id=${id}` });
    }
    const { createMollieClient } = await import("@mollie/api-client");
    const mollie = createMollieClient({ apiKey: opts.mollieApiKey! });
    const payment = await mollie.payments.create({
      amount: { currency: "EUR", value: body.amount },
      description: body.description,
      redirectUrl: opts.publicUrl ?? "http://localhost:5173",
      metadata: { appId: body.appId },
    });
    return c.json({ id: payment.id, checkoutUrl: payment.getCheckoutUrl() });
  });

  app.get("/api/payments/:id", async (c) => {
    const id = c.req.param("id");
    if (opts.paymentsMock) {
      const p = mockPayments.get(id);
      if (!p) return c.body(null, 404);
      p.polls++;
      return c.json({ status: p.polls >= 1 ? "paid" : "open" });
    }
    const { createMollieClient } = await import("@mollie/api-client");
    const mollie = createMollieClient({ apiKey: opts.mollieApiKey! });
    const payment = await mollie.payments.get(id);
    return c.json({ status: payment.status });
  });

  return app;
}
```

`src/index.ts`:
```ts
import { serve } from "@hono/node-server";
import { createApp } from "./app";

const app = createApp({
  authSecret: process.env.AUTH_SECRET ?? "dev-secret-verander-mij",
  paymentsMock: process.env.PAYMENTS_MOCK === "1",
  mollieApiKey: process.env.MOLLIE_API_KEY,
  publicUrl: process.env.PUBLIC_URL,
});
serve({ fetch: app.fetch, port: 5175 });
console.log("[plein-demo-server] http://localhost:5175");
```

- [ ] **Step 4: Run tests — moeten slagen**

Run: `pnpm install && pnpm --filter @openplein/demo-server exec vitest run`
Expected: 3 passed.

- [ ] **Step 5: Vervang de payments-stub in de runtime**

`packages/runtime/src/providers/payments.ts` (volledig vervangen):
```ts
export const paymentsProvider = {
  async pay(appId: string, params: unknown, token: string): Promise<{ status: string }> {
    const res = await fetch("/api/payments", {
      method: "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify({ ...(params as object), appId }),
    });
    if (!res.ok) throw new Error(`Betaling aanmaken mislukt (${res.status})`);
    const { id, checkoutUrl } = (await res.json()) as { id: string; checkoutUrl: string };
    window.open(checkoutUrl, "_blank", "noopener");
    const deadline = Date.now() + 5 * 60_000;
    while (Date.now() < deadline) {
      await new Promise((r) => setTimeout(r, 2000));
      const poll = await fetch(`/api/payments/${id}`, {
        headers: { Authorization: `Bearer ${token}` },
      });
      const { status } = (await poll.json()) as { status: string };
      if (status !== "open") return { status: status === "expired" ? "failed" : status };
    }
    return { status: "failed" };
  },
};
```

- [ ] **Step 6: Handmatige end-to-end-check (mock)**

Run in twee terminals: `PAYMENTS_MOCK=1 pnpm --filter @openplein/demo-server dev` en `pnpm --filter @openplein/runtime dev`.
Expected: inloggen lukt (code staat in de server-stdout); `pnpm typecheck` groen.

- [ ] **Step 7: Commit**

```bash
git add apps/demo/server packages/runtime/src/providers/payments.ts
git commit -m "feat(demo): Hono-backend met magic-code-auth en Mollie/mock-betalingen + runtime-paymentsprovider"
```

---

### Task 9: Demo-mini-app "Lijstje" (identity + storage)

**Files:**
- Create: `apps/demo/miniapps/lijstje/index.html`, `apps/demo/miniapps/lijstje/app.js`, `apps/demo/miniapps/lijstje/plein.manifest.json`, `apps/demo/miniapps/lijstje/icon.svg`, `apps/demo/miniapps/package.json`

**Interfaces:**
- Consumes: `window.plein`-client. Mini-apps bundelen de client niet via npm maar laden `plein-client.js` — voeg daarvoor in `packages/bridge/package.json` een script toe: `"build:client": "esbuild src/client.ts --bundle --format=iife --global-name=PleinBridge --outfile=dist/plein-client.js"` (devDependency `esbuild ^0.21`), en kopieer het bestand naar elke mini-app-map. In de mini-app: `const plein = PleinBridge.createPleinClient()`.
- Produces: statisch gehoste mini-app op poort 5180 (`npx serve` of vite preview — zie package.json hieronder).

- [ ] **Step 1: Hosting-package**

`apps/demo/miniapps/package.json`:
```json
{
  "name": "@openplein/demo-miniapps",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:lijstje": "npx --yes serve -l 5180 lijstje",
    "dev:betalen": "npx --yes serve -l 5181 betalen"
  }
}
```

- [ ] **Step 2: Bouw de mini-app**

`lijstje/plein.manifest.json`: identiek aan het catalogus-item `nl.easeo.lijstje` uit Task 7.

`lijstje/index.html`:
```html
<!doctype html>
<html lang="nl">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Lijstje</title>
<style>
  body { font-family: system-ui, sans-serif; margin: 1rem; }
  li button { margin-left: .5rem; }
</style></head>
<body>
  <h1>🛒 Lijstje</h1>
  <p id="wie">…</p>
  <form id="f"><input id="item" placeholder="Nieuw item"><button>+</button></form>
  <ul id="lijst"></ul>
  <script src="plein-client.js"></script>
  <script src="app.js"></script>
</body>
</html>
```

`lijstje/app.js`:
```js
const plein = PleinBridge.createPleinClient();
const KEY = "items";
let items = [];

async function render() {
  document.getElementById("lijst").innerHTML = items
    .map((it, i) => `<li>${it} <button data-i="${i}">×</button></li>`).join("");
}
async function save() { await plein.storage.set(KEY, JSON.stringify(items)); }

document.getElementById("f").addEventListener("submit", async (e) => {
  e.preventDefault();
  const input = document.getElementById("item");
  if (!input.value.trim()) return;
  items.push(input.value.trim()); input.value = "";
  await save(); render();
});
document.getElementById("lijst").addEventListener("click", async (e) => {
  if (e.target.dataset.i === undefined) return;
  items.splice(Number(e.target.dataset.i), 1);
  await save(); render();
});

(async () => {
  try {
    const { email } = await plein.identity.request();
    document.getElementById("wie").textContent = `Lijstje van ${email}`;
  } catch { document.getElementById("wie").textContent = "Niet ingelogd"; }
  items = JSON.parse((await plein.storage.get(KEY)) ?? "[]");
  render();
})();
```

`icon.svg`: simpel winkelwagen-icoon (mag een `<svg>` met een emoji-`<text>` zijn).

- [ ] **Step 3: Handmatige verificatie in de shell**

Run (drie terminals): demo-server (mock), miniapp (`pnpm --filter @openplein/demo-miniapps dev:lijstje`), runtime dev. Open de shell, log in, open "Lijstje".
Expected: permissiedialoog voor `identity` en daarna `storage`; na toestaan toont de app je e-mail en onthoudt items over herladen heen. Na weigeren: app toont "Niet ingelogd", geen crash.

- [ ] **Step 4: Commit**

```bash
git add apps/demo/miniapps packages/bridge/package.json
git commit -m "feat(demo): mini-app Lijstje (identity+storage) + gebundelde plein-client"
```

---

### Task 10: Demo-mini-app "Betalen" (Nixpay/Mollie-checkout)

**Files:**
- Create: `apps/demo/miniapps/betalen/index.html`, `apps/demo/miniapps/betalen/app.js`, `apps/demo/miniapps/betalen/plein.manifest.json`, `apps/demo/miniapps/betalen/icon.svg` (+ kopie `plein-client.js`)

**Interfaces:**
- Consumes: `plein.pay({ amount, currency: "EUR", description })` → `{ status }` (Task 4/8).

- [ ] **Step 1: Bouw de mini-app**

`betalen/plein.manifest.json`: identiek aan catalogus-item `nl.nixpay.betalen` uit Task 7.

`betalen/index.html`: zelfde skelet als lijstje, titel "Betaal-demo", body:
```html
<h1>☕ Steun het Plein</h1>
<p>Demo-betaling via Nixpay (Mollie-testmodus).</p>
<button id="pay" data-amount="2.50">Doneer € 2,50</button>
<button id="pay10" data-amount="10.00">Doneer € 10,00</button>
<p id="status"></p>
<script src="plein-client.js"></script>
<script src="app.js"></script>
```

`betalen/app.js`:
```js
const plein = PleinBridge.createPleinClient();
const status = document.getElementById("status");

async function pay(amount) {
  status.textContent = "Bezig met betalen…";
  try {
    const { status: s } = await plein.pay({
      amount, currency: "EUR", description: `Donatie Plein-demo € ${amount}`,
    });
    status.textContent = s === "paid" ? "✅ Bedankt voor je steun!" : `Betaling: ${s}`;
  } catch (e) {
    status.textContent = e.code === "PERMISSION_DENIED"
      ? "Je hebt betalen geweigerd." : `Fout: ${e.message}`;
  }
}
document.getElementById("pay").addEventListener("click", (e) => pay(e.target.dataset.amount));
document.getElementById("pay10").addEventListener("click", (e) => pay(e.target.dataset.amount));
```

- [ ] **Step 2: Handmatige verificatie (mock én echt)**

Mock: zoals Task 9 maar open "Betaal-demo" → permissiedialoog `payments` → mock-checkout-tab opent → status wordt "✅ Bedankt". Echt (eenmalig, met Mollie-testkey): `MOLLIE_API_KEY=test_xxx pnpm --filter @openplein/demo-server dev` → echte Mollie-testcheckout verschijnt, kies "Paid".
Expected: beide flows tonen "✅ Bedankt voor je steun!".

- [ ] **Step 3: Commit**

```bash
git add apps/demo/miniapps/betalen
git commit -m "feat(demo): mini-app Betalen met Nixpay/Mollie-testcheckout"
```

---

### Task 11: SDK — `create-plein-app`-scaffolder

**Files:**
- Create: `packages/sdk/src/cli.ts`, `packages/sdk/template/index.html`, `packages/sdk/template/app.js`, `packages/sdk/template/plein.manifest.json`
- Modify: `packages/sdk/package.json` (bin-entry + files)
- Test: `packages/sdk/src/cli.test.ts`

**Interfaces:**
- Produces: `npx create-plein-app <naam>` (bin `create-plein-app` → `src/cli.ts` via tsx/node) — maakt map `<naam>/` met de drie templatebestanden waarin `__NAME__` vervangen is door de naam en `__ID__` door `nl.example.<naam>`; print vervolgstappen (serveren + manifest aanmelden bij een Plein-catalogus).

- [ ] **Step 1: Schrijf de falende test**

`packages/sdk/src/cli.test.ts`:
```ts
import { describe, it, expect, afterEach } from "vitest";
import { scaffold } from "./cli";
import { rmSync, readFileSync, existsSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

const dir = join(tmpdir(), `plein-test-${process.pid}`);
afterEach(() => rmSync(dir, { recursive: true, force: true }));

describe("scaffold", () => {
  it("maakt een mini-app-map met ingevuld manifest", () => {
    scaffold("mijnapp", dir);
    expect(existsSync(join(dir, "index.html"))).toBe(true);
    const manifest = JSON.parse(readFileSync(join(dir, "plein.manifest.json"), "utf8"));
    expect(manifest.id).toBe("nl.example.mijnapp");
    expect(manifest.name).toBe("mijnapp");
  });
  it("weigert een bestaande niet-lege map", () => {
    scaffold("mijnapp", dir);
    expect(() => scaffold("mijnapp", dir)).toThrow(/bestaat al/);
  });
});
```

- [ ] **Step 2: Run — FAIL**, dan implementeer

`packages/sdk/template/plein.manifest.json`:
```json
{
  "id": "__ID__", "name": "__NAME__", "version": "0.1.0",
  "icon": "icon.svg", "entry": "http://localhost:5190/",
  "provider": { "name": "__NAME__", "url": "https://example.eu" },
  "permissions": ["storage"]
}
```

`template/index.html`: minimaal skelet met `<h1>__NAME__</h1>`, `<script src="plein-client.js"></script>` en `<script src="app.js"></script>` (met comment waar `plein-client.js` te vinden is: gebundeld uit `@openplein/bridge`).

`template/app.js`:
```js
const plein = PleinBridge.createPleinClient();
(async () => {
  const eerder = await plein.storage.get("bezocht");
  document.body.insertAdjacentHTML("beforeend",
    eerder ? "<p>Welkom terug!</p>" : "<p>Eerste bezoek 👋</p>");
  await plein.storage.set("bezocht", "ja");
})();
```

`packages/sdk/src/cli.ts`:
```ts
import { cpSync, existsSync, readdirSync, readFileSync, writeFileSync, mkdirSync } from "node:fs";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";

export function scaffold(name: string, targetDir: string): void {
  if (!/^[a-z][a-z0-9-]*$/.test(name)) throw new Error(`Ongeldige naam: ${name}`);
  if (existsSync(targetDir) && readdirSync(targetDir).length > 0)
    throw new Error(`Map ${targetDir} bestaat al en is niet leeg`);
  mkdirSync(targetDir, { recursive: true });
  const templateDir = join(dirname(fileURLToPath(import.meta.url)), "..", "template");
  cpSync(templateDir, targetDir, { recursive: true });
  for (const file of readdirSync(targetDir)) {
    const p = join(targetDir, file);
    const content = readFileSync(p, "utf8")
      .replaceAll("__NAME__", name)
      .replaceAll("__ID__", `nl.example.${name}`);
    writeFileSync(p, content);
  }
  console.log(`✔ Mini-app '${name}' aangemaakt in ${targetDir}
Volgende stappen:
  1. Kopieer plein-client.js (gebundeld uit @openplein/bridge) naar de map
  2. Serveer de map, bv.: npx serve -l 5190 ${targetDir}
  3. Voeg het manifest toe aan de catalog.json van je Plein-runtime`);
}

// CLI-entry
const invokedDirectly = process.argv[1]?.endsWith("cli.ts") || process.argv[1]?.endsWith("create-plein-app");
if (invokedDirectly) {
  const name = process.argv[2];
  if (!name) { console.error("Gebruik: create-plein-app <naam>"); process.exit(1); }
  scaffold(name, join(process.cwd(), name));
}
```

In `packages/sdk/package.json` toevoegen:
```json
"bin": { "create-plein-app": "./src/cli.ts" },
"files": ["src", "template"]
```

- [ ] **Step 3: Run tests — moeten slagen**

Run: `pnpm --filter @openplein/sdk exec vitest run`
Expected: 5 passed (3 manifest + 2 cli).

- [ ] **Step 4: Commit**

```bash
git add packages/sdk && git commit -m "feat(sdk): create-plein-app-scaffolder met template"
```

---

### Task 12: E2E-tests en sandbox-escape-checks (Playwright)

**Files:**
- Create: `e2e/package.json`, `e2e/playwright.config.ts`, `e2e/tests/shell.spec.ts`, `e2e/tests/sandbox.spec.ts`

**Interfaces:**
- Consumes: alle lopende services; de config start ze zelf via `webServer` (demo-server met `PAYMENTS_MOCK=1`, beide mini-apps, runtime dev).

- [ ] **Step 1: Setup**

`e2e/package.json`:
```json
{
  "name": "@openplein/e2e", "private": true, "type": "module",
  "scripts": { "test": "playwright test" },
  "devDependencies": { "@playwright/test": "^1.45.0" }
}
```

`e2e/playwright.config.ts`:
```ts
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "tests",
  use: { baseURL: "http://localhost:5173" },
  webServer: [
    { command: "PAYMENTS_MOCK=1 AUTH_SECRET=e2e pnpm --filter @openplein/demo-server start", port: 5175, reuseExistingServer: true },
    { command: "pnpm --filter @openplein/demo-miniapps dev:lijstje", port: 5180, reuseExistingServer: true },
    { command: "pnpm --filter @openplein/demo-miniapps dev:betalen", port: 5181, reuseExistingServer: true },
    { command: "pnpm --filter @openplein/runtime dev", port: 5173, reuseExistingServer: true },
  ],
});
```

- [ ] **Step 2: Shell-flow-test**

`e2e/tests/shell.spec.ts` — let op: de login-code wordt in mock/dev naar stdout gelogd; voor E2E maken we hem bereikbaar door in Task 8's `createApp` óók `app.debugLastCode` te zetten en (alleen wanneer `AUTH_SECRET=e2e`) een endpoint `GET /api/auth/debug-last-code` → `{ code }` te exposen. Voeg dat endpoint in deze task toe aan `apps/demo/server/src/app.ts`:
```ts
if (opts.authSecret === "e2e") {
  app.get("/api/auth/debug-last-code", (c) => c.json({ code: app.debugLastCode ?? "" }));
}
```

```ts
import { test, expect } from "@playwright/test";

async function login(page) {
  await page.goto("/");
  await page.getByLabel(/E-mail/i).fill("e2e@plein.test");
  await page.getByRole("button", { name: /Stuur code|Send code/i }).click();
  const { code } = await (await page.request.get("http://localhost:5175/api/auth/debug-last-code")).json();
  await page.getByLabel(/Code/i).fill(code);
  await page.getByRole("button", { name: /Inloggen|Sign in/i }).click();
}

test("volledige flow: login → lijstje → permissies → betaling (mock)", async ({ page, context }) => {
  await login(page);
  await expect(page.getByRole("heading", { name: "Plein" })).toBeVisible();

  await page.getByRole("button", { name: /Lijstje/ }).click();
  await page.getByRole("button", { name: /Toestaan|Allow/ }).click(); // identity
  await page.getByRole("button", { name: /Toestaan|Allow/ }).click(); // storage
  const frame = page.frameLocator("iframe");
  await expect(frame.getByText("Lijstje van e2e@plein.test")).toBeVisible();

  await page.getByRole("button", { name: /Sluiten|Close/ }).click();
  await page.getByRole("button", { name: /Betaal-demo/ }).click();
  const frame2 = page.frameLocator("iframe");
  await frame2.getByRole("button", { name: /2,50/ }).click();
  await page.getByRole("button", { name: /Toestaan|Allow/ }).click(); // payments
  await expect(frame2.getByText(/Bedankt voor je steun/)).toBeVisible({ timeout: 15_000 });
});
```

- [ ] **Step 3: Sandbox-escape-tests**

`e2e/tests/sandbox.spec.ts`:
```ts
import { test, expect } from "@playwright/test";

test("mini-app-iframe heeft geen allow-same-origin en kan niet bij de shell", async ({ page }) => {
  // login-helper hergebruiken zoals in shell.spec.ts
  await page.goto("/");
  await page.getByLabel(/E-mail/i).fill("e2e@plein.test");
  await page.getByRole("button", { name: /Stuur code|Send code/i }).click();
  const { code } = await (await page.request.get("http://localhost:5175/api/auth/debug-last-code")).json();
  await page.getByLabel(/Code/i).fill(code);
  await page.getByRole("button", { name: /Inloggen|Sign in/i }).click();
  await page.getByRole("button", { name: /Lijstje/ }).click();

  const sandbox = await page.locator("iframe").getAttribute("sandbox");
  expect(sandbox).toContain("allow-scripts");
  expect(sandbox).not.toContain("allow-same-origin");

  // een mini-app die parent-DOM probeert te lezen moet een SecurityError krijgen
  const escaped = await page.frameLocator("iframe").locator("body").evaluate(() => {
    try { return window.parent.document.title; } catch { return "GEBLOKKEERD"; }
  });
  expect(escaped).toBe("GEBLOKKEERD");

  // localStorage van de shell is onbereikbaar (opaque origin gooit bij toegang)
  const storage = await page.frameLocator("iframe").locator("body").evaluate(() => {
    try { return localStorage.getItem("plein.session") ?? "LEEG"; } catch { return "GEBLOKKEERD"; }
  });
  expect(storage).toBe("GEBLOKKEERD");
});
```

- [ ] **Step 4: Run**

Run: `pnpm install && pnpm --filter @openplein/e2e exec playwright install chromium && pnpm --filter @openplein/e2e test`
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add e2e apps/demo/server && git commit -m "test(e2e): volledige shell-flow + sandbox-escape-checks"
```

---

### Task 13: Docs — README, GOVERNANCE, mini-app-spec, funding-map

**Files:**
- Create: `README.md`, `GOVERNANCE.md`, `docs/miniapp-spec.md`, `docs/funding/README.md`

- [ ] **Step 1: Schrijf de documenten**

`README.md` (NL met Engelse samenvatting bovenaan): wat is OpenPlein (open-source mini-app-platform voor NL/EU, "Jouw diensten, één Plein"), quickstart (pnpm install, drie dev-commando's, inloggen met code uit stdout), architectuurdiagram in mermaid (shell → bridge → providers), link naar `docs/miniapp-spec.md`, licentiesectie (AGPL runtime/bridge, MIT sdk), attributie: "Een initiatief van Sovereign AI Grid — founding payment provider: Nixpay".

`GOVERNANCE.md`: maintainer Nixpay B.V./SAIG; open roadmap via GitHub-issues; contributies welkom onder DCO; belofte: bij structurele groei van het contributor-bestand migreert het project naar een onafhankelijke stichting; besluitvorming tot die tijd: maintainer beslist, RFC-issues voor breaking changes aan het manifest of de bridge.

`docs/miniapp-spec.md`: het manifest-formaat (kopieer het JSON Schema-veldoverzicht uit Task 2), de bridge-API (`pay`, `identity.request`, `storage.get/set` met request/response-voorbeelden), het permissiemodel (eerste gebruik = dialoog, beslissing persistent), de sandbox-eisen (iframe zonder allow-same-origin; alle netwerk-calls naar de eigen origin van de aanbieder), en de fase-1/2-roadmap (notifications, Matrix, EUDI, registry).

`docs/funding/README.md`: skelet voor de NGI Zero Commons Fund-aanvraag met de secties uit het NLnet-formulier (Abstract ≤1200 tekens, "Have you been involved with projects or organisations relevant to this project before?", "Requested amount" + breakdown, "Compare your own project with existing or historical efforts", technisch plan, ecosysteem) — elk met 2-3 zinnen aanzet vanuit de spec (soevereiniteitsverhaal, compositie van EU-bouwstenen, AGPL/MIT-model).

- [ ] **Step 2: Verifieer + commit**

Run: `pnpm typecheck && pnpm test`
Expected: alles groen.

```bash
git add README.md GOVERNANCE.md docs && git commit -m "docs: README, governance, mini-app-spec en NGI-aanvraagskelet"
```

---

### Task 14: Deploy naar plein.sovereignaigrid.nl (VPS, Docker + Caddy)

**Files:**
- Create: `Dockerfile`, `docker-compose.yml`, `deploy.md`

**Interfaces:**
- Consumes: productie-builds van runtime (Vite) en demo-server; Nicks bestaande VPS-patroon (Hostinger-VPS 88.222.220.64, Caddyfile op `/opt/docker/core/caddy/`, docker compose per service onder `/opt/docker/`).

- [ ] **Step 1: Dockerfile (multi-stage)**

```dockerfile
FROM node:20-alpine AS build
RUN corepack enable
WORKDIR /app
COPY . .
RUN pnpm install --frozen-lockfile && pnpm --filter @openplein/runtime build \
 && pnpm --filter @openplein/bridge build:client \
 && cp packages/bridge/dist/plein-client.js apps/demo/miniapps/lijstje/ \
 && cp packages/bridge/dist/plein-client.js apps/demo/miniapps/betalen/

FROM node:20-alpine
RUN corepack enable
WORKDIR /app
COPY --from=build /app .
EXPOSE 5175
CMD ["pnpm", "--filter", "@openplein/demo-server", "start"]
```

De demo-server serveert in productie óók de statische bestanden — voeg in `apps/demo/server/src/app.ts` toe (Hono `serveStatic` uit `@hono/node-server/serve-static`): runtime-`dist/` op `/`, mini-apps op `/miniapps/lijstje` en `/miniapps/betalen`. Pas `catalog.json` voor productie aan zodat `entry`/`icon` naar `https://plein.sovereignaigrid.nl/miniapps/...` wijzen (aparte `catalog.prod.json`, gekopieerd bij de build).

**Let op sandbox-consequentie:** in productie draaien mini-apps op dezelfde origin als de shell; de iframe-sandbox zonder `allow-same-origin` maakt hun origin opaque, dus de isolatie blijft intact — dit is exact waarom die sandbox-vlag een Global Constraint is.

- [ ] **Step 2: docker-compose.yml**

```yaml
services:
  openplein:
    build: .
    restart: unless-stopped
    environment:
      AUTH_SECRET: ${AUTH_SECRET}
      MOLLIE_API_KEY: ${MOLLIE_API_KEY}
      PUBLIC_URL: https://plein.sovereignaigrid.nl
    ports:
      - "127.0.0.1:5175:5175"
```

- [ ] **Step 3: Deploy + Caddy-route**

`deploy.md` beschrijft: rsync/`git clone` naar VPS `/opt/docker/openplein/`, `.env` met echte `AUTH_SECRET` + Mollie-**test**key, `docker compose up -d --build`, en in de Caddyfile op `/opt/docker/core/caddy/`:
```
plein.sovereignaigrid.nl {
    reverse_proxy openplein:5175
}
```
plus DNS-A-record `plein` → 88.222.220.64 en `docker compose -f /opt/docker/core/... restart caddy` (het bestaande reload-patroon van de VPS).

- [ ] **Step 4: Verifieer productie**

Open `https://plein.sovereignaigrid.nl`: login met code (uit `docker compose logs`), Lijstje-flow, betaal-flow met Mollie-testkey.
Expected: alles werkt zoals lokaal; PWA-install-prompt beschikbaar (HTTPS).

- [ ] **Step 5: Commit**

```bash
git add Dockerfile docker-compose.yml deploy.md apps/demo/server
git commit -m "feat(deploy): Docker-build + Caddy-route voor plein.sovereignaigrid.nl"
```

---

## Definition of Done (fase 0)

- `pnpm test` groen (sdk 5, bridge 11, runtime 4, demo-server 3) en `pnpm --filter @openplein/e2e test` groen (2).
- Handmatig: login → Lijstje (identity+storage, permissiedialogen, persistentie) → Betaal-demo met échte Mollie-testkey → "Bedankt".
- PWA installeerbaar (Chrome "Install app" beschikbaar op localhost).
- README-quickstart klopt letterlijk (uitproberen in schone checkout).
- Live op `https://plein.sovereignaigrid.nl` (Task 14) met werkende login- en betaalflow.





